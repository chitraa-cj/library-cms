node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local / machine-only (never push — GitHub rejects >100MB)
.local/
logs/

# Cursor / IDE local narrative (not application source)
.specstory/

# Secrets — use server env or CI secrets only
.env
.env.*
!.env.example